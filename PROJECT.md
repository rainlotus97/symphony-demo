# Symphony Demo 项目说明

## 项目简介

Symphony Demo 是一个用于展示 Symphony 自主编码工作流的轻量级静态网站示例。项目不依赖构建工具或第三方库，适合快速验证页面修改、自动化协作和候选分支交付流程。

## 功能

- 展示项目欢迎信息和自主编码工作流简介
- 支持浅色与深色主题切换
- 使用浏览器 `localStorage` 保存主题选择
- 通过语义化 HTML、独立 CSS 和原生 JavaScript 实现

## 目录结构

| 文件 | 说明 |
| --- | --- |
| `index.html` | 页面结构和主题初始化逻辑 |
| `style.css` | 页面布局、颜色和主题样式 |
| `app.js` | 主题切换及本地存储逻辑 |
| `README.md` | 项目的简要介绍 |
| `CLAUDE.md` | 面向代码协作的项目约定 |

## 运行方式

项目没有构建步骤。直接使用浏览器打开 `index.html` 即可预览；修改 HTML、CSS 或 JavaScript 后刷新页面即可看到结果。

## 开发约定

- 保持项目为无外部依赖的静态文件项目。
- 页面结构写入 `index.html`，样式写入 `style.css`，交互逻辑写入 `app.js`。
- 主题状态使用键名 `symphony-theme` 保存。
- 修改后至少在浏览器中检查页面加载和主题切换功能。

